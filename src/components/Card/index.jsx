import styled from 'styled-components';

const CardContainer = styled.div`
  width: 100vw;
  height: 100vh;
  background-color: white;
  text-align: center;
`

function Card() {
  return (
    <CardContainer>
    </CardContainer>
  )
}

export default Card
